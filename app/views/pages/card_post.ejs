<% include ../partials/head %>

<body>
    <% include ../partials/global_nav %>

    <div class="container">
        <h1><%= page_title %></h1>
		<form method="post" action="<%= typeof card=='undefined' ? '/cards/post' : '/cards/'+card._id+'/edit' %>"  enctype="multipart/form-data">
			
			<div class="form-group">
				<label for="title">Name your card</label> <br>  
				<input type="text" 
					name="title" 
					class="form-control" 
					id="title" 
					placeholder="Something fun" 
					value="<%= typeof card!='undefined' ? card.title : '' %>">
			</div>


			<div class="form-group">
				<label for="description">Describe your card</label><br>
				<textarea id="description" name="description" class="form-control"><%= typeof card!='undefined' ? card.description : '' %></textarea>
			</div>
				

			<div class="form-group">
				<label for="price">Price</label><br>
				<input type="text" name="price" class="form-control" id="price" placeholder="$" value="<%= typeof card!='undefined' ? card.attr.price : '' %>">
			</div>


			<div class="form-group">
				<% if(typeof card!='undefined') { %>
					<div>
						<img src="<%= card.image %>" alt="<%= card.title %>" />
					</div>
				<% } %>

				<label for="image">File</label><br>
				<input type="file" id="image" name="image" />
			</div>

			<br><br><br>

			<div class="form-group">
				<input type="submit" value="Submit" class="btn btn--primary">
			</div>
		</form>

    </div>

    <% include ../partials/footer %>

    <script src="jquery/dist/jquery.js"></script>
    <script src="bootstrap/dist/js/bootstrap.js"></script>

</body>
</html>